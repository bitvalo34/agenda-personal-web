# Database connection (Docker Compose servicio “db”)
DB_HOST=db
DB_USER=agenda_user
DB_PASS=agenda_pwd
DB_NAME=agenda_web

# JWT secret
JWT_SECRET=unaClaveMuySecreta

# SMTP real (Hostinger)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=no-reply@tudominio.com
SMTP_PASS=ContraseñaDelCorreo

# Opcional: URL base para email links
BASE_URL=https://api.tudominio.com

